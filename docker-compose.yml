version: '3.8'
services:
    frontend:
        build:
            context: ./
            dockerfile: Dockerfile
        container_name: frontend
        ports:
            - '80:80'
            - '443:443'
        volumes:
            - ./build:/var/www/html
            - ./nginx/nginx.conf:/etc/nginx/sites-available/default
